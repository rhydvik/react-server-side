/* @flow */
import React from 'react';

import styles from './styles.module.scss';

const Banner = () => (
  <div className={styles.container}>
    <div className={styles.textBox}>
      <p className={styles.bannerText}>SOME DUMMY TEXT</p>
    </div>
  </div>
);

export default Banner;
